apiVersion: v2
name: okg
description: OKG knowledge-graph runtime (Postgres + runtime StatefulSet + MCP) — archi-crab vendored revision
type: application
version: 0.5.0
appVersion: "bed964f"   # upstream mitdbg/okg commit the pinned images were cut from
